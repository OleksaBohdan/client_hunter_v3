import { Box } from '@mui/material';

const Parser = () => {
  return (
    <Box sx={{ width: '100%', justifyContent: 'center', backgroundColor: 'lightblue' }}>
      <Box sx={{ textAlign: 'center' }}>PARSER</Box>
    </Box>
  );
};

export default Parser;
