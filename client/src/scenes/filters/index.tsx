import { Box } from '@mui/material';

const Filters = () => {
  return (
    <Box sx={{ width: '100%', justifyContent: 'center', backgroundColor: 'lightcoral' }}>
      <Box sx={{ textAlign: 'center' }}>FILTERS</Box>
    </Box>
  );
};

export default Filters;
